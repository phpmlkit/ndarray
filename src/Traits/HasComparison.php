<?php

declare(strict_types=1);

namespace NDArray\Traits;

/**
 * Element-wise comparison operations.
 *
 * Will provide eq(), ne(), gt(), ge(), lt(), le(),
 * all(), any(), where(), etc.
 */
trait HasComparison
{
}
