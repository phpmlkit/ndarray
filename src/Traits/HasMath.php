<?php

declare(strict_types=1);

namespace NDArray\Traits;

/**
 * Element-wise and aggregate math operations.
 *
 * Will provide add(), subtract(), multiply(), divide(), matmul(),
 * sum(), mean(), min(), max(), abs(), sqrt(), exp(), log(), etc.
 */
trait HasMath
{
}
